# jianji 项目交付清单（Deliverables Manifest）

> 编制日期：2026-08-05
> 数据来源：**GitHub API 实测**（release 列表、tag 指向 commit 的 tree、远端 main 文件存在性），非文档推演。
> 验证原则：每一项交付的「验证方式」均给出**可复现的命令/证据锚点**（CI run 号、commit SHA、release URL、grep/tree 查询），任何人可执行核查，不接受空口"已完成"。

---

## 一、已发布版本（Release）

| 版本 | versionCode | 发布日期(UTC) | 负责人 | 交付状态 | 验证方式 / 可验证指标 |
|---|---|---|---|---|---|
| v1.6.28 | 61 | 2026-08-04 12:58 | AI Agent | ✅ 已发布 | Release URL: https://github.com/gnaiq/jianji/releases/tag/v1.6.28 ；修复 Room 迁移闪退 |
| v1.6.29 | 62 | 2026-08-04 13:19 | AI Agent | ✅ 已发布 | URL: /tag/v1.6.29 ；含备份加密口令 UI 闭环（T-1 实测完成，详见第二节 D-07） |
| v1.6.30 | 63 | 2026-08-04 15:06 | AI Agent | ✅ 已发布 | URL: /tag/v1.6.30 ；vc=63 已由 `git tree @v1.6.30` 核实 |
| v1.6.31 | 64 | 2026-08-04 15:42 | AI Agent | ✅ 已发布 | URL: /tag/v1.6.31 ；vc=64 核实；路线图原基线版本 |
| v1.6.32 | 65 | 2026-08-04 17:21 | AI Agent | ✅ 已发布 | URL: /tag/v1.6.32 ；vc=65 核实；曾作为 Wave E 拆包临时载体 |
| **v1.6.33** | **66** | **2026-08-05 03:27** | **AI Agent** | **🟡 已发布（含已知残留缺陷）** | **见下方 D-00 专项核查** |

> 版本号连续性验证：`for v in v1.6.29..v1.6.33; do gh api contents/app/build.gradle.kts?ref=$v | base64 -d | grep versionCode; done` → 62/63/64/65/66 严格递增 ✅（CI 严格递增守卫保证）。

---

## 二、v1.6.33 交付专项核查（D-00，须重点复核）

**发布事实（已核实）**
- Tag：`v1.6.33` → commit `359deffafb6987fccbaa360b56a8638df910e02b`
- Release：id=365270588，asset `jianji-v1.6.33.apk`，URL https://github.com/gnaiq/jianji/releases/tag/v1.6.33
- Release job：`30972013400` → conclusion **success**
- versionCode=66 / versionName="1.6.33"（tag commit tree 核实 ✅）

**交付项明细与验证**

| 编号 | 交付项 | 状态 | 验证方式 / 客观指标 |
|---|---|---|---|
| D-01 | utils→core 拆包（E-2） | 🟡 **部分交付** | 新包已就位：`gh api contents/app/src/main/java/com/example/jianji/core/common/AppPrefs.kt?ref=v1.6.33` → 200 ✅；但旧 `utils/` **残留 15 个文件**（见 D-02），拆包未彻底 |
| D-02 | 清空 `utils/` 旧文件 | ❌ **未交付（缺陷）** | `gh api git/trees/359deffa?recursive=1 \| grep -c "src/main/java/com/example/jianji/utils/"` → **15**（应为 0）。15 个残留：`AppPrefs/DateUtils/VersionComparator/StatisticsCalculator/RecurringSchedule/PosterGenerator`（common）+ `BackupCrypto/BackupStorage/AutoBackup/AutoBackupWorker/BackupScheduler/DataImportManager/DataExportManager`（backup）+ `UpdateManager`（update）+ `ExcelExportManager`（io）|
| D-03 | ProGuard `-keep` 同步为 `core.**`（E-3） | 🟡 已写入但风险未消 | `gh api contents/app/proguard-rules.pro?ref=v1.6.33 \| base64 -d \| grep "core.\*\*" ` → 存在 ✅；但因 D-02 残留 `utils.*` 仍被 keep，`utils`+`core` 双份同类并存 |
| D-04 | 删除 `TransactionViewModelFactory`（E-1） | ✅ 已交付 | `gh api contents/.../TransactionViewModelFactory.kt?ref=v1.6.33` → **404 Not Found** ✅ |
| D-05 | 补 `PosterGenerator`/`BackupFileEntry` import | ✅ 已交付 | build job `30970850730`(失败)→`30971289492`(失败)→`30971705558`(success) 迭代修复后绿；源码 `SettingsScreen.kt`/`SettingsDialogs.kt`/`BackupDialogs.kt` import 段核实 |
| D-06 | 补 `FakeDao.observeAccountBalances()` | ✅ 已交付 | `TransferValidationTest.kt` 内含 override；build success 即证明编译通过 |
| D-07 | 备份加密口令 UI（T-1） | ✅ 已交付（历史 v1.6.29） | `gh api contents/app/src/main/java/com/example/jianji/ui/.../BackupPassphraseDialog.kt?ref=main` 存在；`backup-encryption-design.md` §6 与 `04` 状态冲突已以代码为准收口 |

**⚠️ v1.6.33 已知缺陷（须回归修正）**
- **D-02：15 个 `utils/*.kt` 残留于发布包**。后果：release 包中 `utils` 与 `core` 两套同名类并存；若 ProGuard 同时 keep 两者，类冲突/方法数膨胀；若只 keep `core`，`utils` 被混淆裁剪但代码仍编译引用 → 潜在运行时崩溃。
- 影响面：理论上 v1.6.33 的真机"备份导出/导入"路径可能因 `utils` 残留类与 `core` 类冲突而出错（PR-2 ProGuard 风险真实触发）。
- **复测命令（任何人可核查）**：
  ```bash
  TAG=v1.6.33
  SHA=$(gh api "git/refs/tags/$TAG" --jq '.object.sha')
  echo "utils 残留数(应为0): $(gh api "git/trees/$SHA?recursive=1" --jq '.tree[].path' | grep -c 'src/main/java/com/example/jianji/utils/')"
  echo "core 存在数(应>0): $(gh api "git/trees/$SHA?recursive=1" --jq '.tree[].path' | grep -c 'src/main/java/com/example/jianji/core/')"
  ```
- **回归动作（建议下一版本 v1.6.34 必修）**：用 Git Data API 删除 `utils/` 下 15 个残留文件 + 复测 release 备份路径真机回归（CT-3）。

---

## 三、代码层交付项（非版本，按模块）

| 编号 | 交付项 | 状态 | 负责人 | 验证方式 |
|---|---|---|---|---|
| C-01 | `core/` 分层包结构（common/backup/update/io） | ✅ 已交付 | AI Agent | `gh api git/trees/main?recursive=1 \| grep -c 'core/'` > 0 ✅ |
| C-02 | `data/` 扁平结构（实体/DAO/仓库） | ✅ 已交付 | AI Agent | `app/src/main/java/com/example/jianji/data/` 存在 ✅；`data/local/` 分层 ❌ 未做（见待办 U-12 类）|
| C-03 | `AccountBalance` 聚合类型 | ✅ 已交付 | AI Agent | `gh api contents/app/src/main/java/com/example/jianji/data/AccountBalance.kt?ref=main` → 200 ✅ |
| C-04 | 迁移测试 `Migration7to8Test` | ✅ 已交付 | AI Agent | `app/src/androidTest/.../Migration7to8Test.kt` 存在 ✅ |
| C-05 | 迁移测试 `Migration8to9Test` | ✅ 已交付 | AI Agent | 同上路径存在 ✅（schema v8→v9 覆盖）|
| C-06 | `TransferValidationTest`（含 FakeDao） | ✅ 已交付 | AI Agent | 存在 ✅；CI `testDebugUnitTest` 绿即证明 |

---

## 四、待交付项（未发布，代码实测）

| 编号 | 待办项 | 优先级 | 负责人 | 当前状态 | 验证方式（完成判据） |
|---|---|---|---|---|---|
| P-01 | 清空 `utils/` 残留 15 文件（D-02 回归） | 🔴 高（v1.6.33 缺陷） | AI Agent | ❌ 未做 | `git/trees/<new tag>?recursive=1 \| grep -c 'utils/'` = 0 |
| P-02 | E-4 建 `domain/` 层 | 中 | AI Agent | ⬜ 未启动（`domain/` 不存在） | `ls app/src/main/java/com/example/jianji/domain` 非空 |
| P-03 | E-5 `data/` → `data/local/{entity,dao}` 分层 | 中 | AI Agent | ⬜ 未启动 | `data/local/` 目录存在且 Room KSP 通过 |
| P-04 | U-10 巨型文件收敛（AddTransactionDialog 825行等 ≤400） | 低 | AI Agent | ⬜ 未启动 | `wc -l` 各文件 ≤400 |
| P-05 | U-11 周期算法抽层（前提已变，须按 `core/` 重评估） | 低 | AI Agent | ⚠️ 待勘误 | `RecurringSchedule` 类位置重定位后抽 `domain/usecase` |
| P-06 | T-2 口令不可找回提示文案 | 低 | AI Agent | ⬜ 未启动（T-1 已完成故可独立做） | 设置页 UI 含二次确认 + 不可找回提示 |
| P-07 | T-3 全链迁移测试补齐（1→6 历史缺基线） | 中 | AI Agent | 🟡 部分（仅 7→8/8→9） | `app/src/androidTest/.../Migration*Test.kt` 数量 ≥6 |
| P-08 | T-4 P3 清理：余额 SQL 聚合 + 移除 kotlinx-serialization | 低 | AI Agent | ⬜ 未启动 | `build.gradle.kts` 无 kotlinx-serialization 依赖；余额走 SQL |
| P-09 | T-5 验收报告补录 v1.6.29–v1.6.33 | 低 | AI Agent | ⬜ 未启动 | `缺陷验收报告.md` 含 v1.6.33 段落 |
| P-10 | U-12 PHASE4 金额 Long 分迁移（最高风险） | 🔴 高（须最后） | AI Agent | 🟡 设计完/执行未启 | 真机回归硬门禁：旧版库升级后数据无丢失 |

---

## 五、交付纪律与核查门禁（PUA 显性化）

1. **不自欺原则**：本次编制发现 v1.6.33 实际**未清空 `utils/`**（D-02 缺陷），已在第二节如实标注，不粉饰为"完全交付"。
2. **客观证据锚点**：每项状态均附 `gh api` 命令或 CI run 号，复核人可执行复现，不依赖作者口头声明。
3. **发布门禁（PR-2）**：ProGuard 与包移动必须同批次；拆包未清空旧包即发版 = release 备份崩溃风险。v1.6.33 已触发该风险（D-02），下一版本必须修复后再发。
4. **版本号纪律**：严格递增守卫（CI 保证 vc 62→66 连续）；同一版本号不重用，发版后不可覆盖。
5. **负责人说明**：全部交付项为 AI Agent 在 jianji 项目中自主执行；用户为验收方。凡标注 🟡/❌ 项均需用户确认是否纳入下一冲刺。

---
*生成方式：基于 `gh api` 对 gnaiq/jianji 仓库的 release/tag/tree 实测；所有状态均可在 2026-08-05 后复现核查。*
